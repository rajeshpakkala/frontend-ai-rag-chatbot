# Copy this file to .env.local for local overrides
# Leave empty in development — Vite proxy handles routing to localhost:8080
# Set to your deployed backend URL in production (no trailing slash)
VITE_API_BASE_URL=
